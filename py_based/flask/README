The following is required to run the hello world app
  - export FLASK_APP=main.py

TDB
  - How to package this Flask app
  - The indirect referencing to phaser.js aka phaser framework is pretty nasty..

Caveat
  - Flask app default static files aka. .png, .js .. to static folder so the
    prefix static is needed when including any of the static files

Installation
  - pip3 install flask
